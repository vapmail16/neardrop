/** Root shell: full viewport + page background (all portals). */
export const ndAppShellRoot = 'min-h-screen bg-surface-page';

/** Constrained main column: same max width and padding everywhere. */
export const ndAppShellMain = 'mx-auto w-full max-w-content px-4 py-6 sm:px-6 sm:py-8';

/** Marketing / auth narrow column. */
export const ndAppShellNarrow = 'mx-auto w-full max-w-md px-4 py-8 sm:px-6';
